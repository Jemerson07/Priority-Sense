import { mockDashboardMetrics } from './mockDashboardMetrics';
import { mockRealtimeFeed } from './mockRealtimeFeed';

export const dashboardOverviewMock = {
  metrics: mockDashboardMetrics,
  events: mockRealtimeFeed,
};
